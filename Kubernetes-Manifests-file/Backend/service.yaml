apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: student-anett
  labels:
    app: backend
spec:
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
  type: ClusterIP
  selector:
    role: api
